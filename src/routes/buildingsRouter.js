const express = require('express');
const {check} = require('express-validator');

//importing buildings-controller business logic handler 
const buildingsRouter = require('')

const route = express.Router();


route.post('/register',)
route.post('/register',)
route.post('/register',)

module.exports = route;
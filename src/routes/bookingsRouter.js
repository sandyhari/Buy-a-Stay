const express = require('express');
const {check} = require('express-validator');

//importing bookings-controller business logic handler 
const bookingsRouter = require('')

const route = express.Router();


route.post('/register',)
route.post('/register',)
route.post('/register',)

module.exports = route;
const express = require('express');
const {check} = require('express-validator');

//importing feedback-controller business logic handler 
const feedBackRouter = require('')

const route = express.Router();


route.post('/register',)
route.post('/register',)
route.post('/register',)

module.exports = route;